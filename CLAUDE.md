# GG 경매 시뮬레이터 (가제)

지지옥션의 **실제 법원경매 과거 데이터**(감정가, 최저가, 실제 낙찰가, 응찰자 수, 물건 사진)를 기반으로 한
스팀(Steam) 출시용 부동산 경매 시뮬레이션 게임.

핵심 루프: 실제 과거 물건을 보고 → 입찰가를 써내고 → 실제 낙찰 결과와 비교 →
낙찰 시 취득세·명도비용 등 **실제와 동일한 비용 구조**로 손익 정산.

## 기술 스택

- **엔진**: Godot 4 (GDScript), gl_compatibility 렌더러 (저사양·빠른 시작)
- **데이터**: `data/*.json` — 게임 로직과 데이터/세율은 완전 분리. 세율·비용 규칙은 코드에 하드코딩 금지, 반드시 `data/cost_rules.json`에서 로드
- **폰트**: Noto Sans KR (Godot 기본 폰트는 한글 미지원 — 반드시 유지)
- **스팀 연동**: 추후 GodotSteam. 지금은 만들지 않는다 (YAGNI)

## 코드 룰 — Ponytail (ponytail.dev) 방식

숙련된 개발자처럼: 말은 줄이고, 최소한의 코드만 쓰고, 동작하게 한다.
코드를 추가하기 전에 반드시 이 사다리(The Ladder)를 순서대로 통과할 것:

1. **정말 필요한가?** 지금 요구사항에 없으면 만들지 않는다 (YAGNI)
2. **이미 이 코드베이스에 있는가?** 있으면 재사용
3. **Godot 내장 기능으로 되는가?** (내장 노드/시그널/리소스 우선, 커스텀 클래스 최소화)
4. **한 줄로 되는가?** 그러면 한 줄로
5. 그 이후에야 최소한의 새 코드를 작성한다

금지: 추측성 추상화, 미래 대비용 옵션 파라미터, 사용처 없는 헬퍼/매니저 클래스,
불필요한 씬 분리. 파일 수와 줄 수는 적을수록 좋다.

## 도메인 규칙 (중요)

- 금액 단위는 **원(₩) 정수**. 부동소수점 금액 연산 금지
- 낙찰 판정: 플레이어 입찰가 > 실제 낙찰가 → 플레이어 낙찰(플레이어 가격으로), 아니면 패찰
- 세율·비용 수치는 지지옥션 측(도메인 전문가)이 `cost_rules.json`으로 검수·수정한다.
  코드는 규칙을 해석만 하고 수치를 소유하지 않는다
- 실데이터 형식은 `DATA_SPEC.md` 참조 — 필드 추가 시 스펙 문서도 갱신

## 실행

Godot 에디터로 `project.godot` 열기 → F5. 엔진은 `tools/` 아래 (레포 미포함).
